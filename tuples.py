## Tuples
# similar to lists
# but key differences
# create tuples >> ()
coordinates = (4, 5)
print(coordinates[0])

# tuples are immutable (cannot change...)
# coordinates[1] = 10 # error
# use for data that never gonna change!

# list of tuples
coordinates = [(4, 5), (6, 7), (8, 9)]
print(coordinates[1])
print(len(coordinates[1]))


